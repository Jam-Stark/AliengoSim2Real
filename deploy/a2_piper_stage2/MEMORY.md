# A2 + PiPER Stage2 Deployment Memory

Updated: 2026-08-25 HKT

本目录记录 policy host 上的 LMP Stage2 dual-policy direct runtime。A2 与 main 成功 locomotion 路径一样在 C++ 进程内复用 `A2LowLevelInterface`，PiPER USB-CAN 仍由 `ros2/Piper/` 的 PC2 bridge 独占。

当前正式 policy host 是 `baoquanc@ai-precog-m45`：Ubuntu 24.04.4、RTX 5070 Ti、robot NIC `enp130s0 / 192.168.123.222/24`。Session `20260824_173406` 的offline、network、physical和ros-readonly均已人工批准；PC2 Docker/CAN/command-gate-closed bridge已完成，实时PiPER joint state为50 Hz。2026-08-24 21:05 HKT，现场操作员在只读观察下逐一移动关节，确认A2前12轴raw index/label与PiPER `arm_j1..arm_j6`映射全部PASS；该记录不等同于Gate 8的direction/unit/zero/limits/stop表或`joint-validation` receipt。真实主机档案和evidence见`docs/policy_host_m45.md`。

2026-08-25 00:51 HKT，最终dual-policy实机run成功完成first-A自动单次PiPER `resume→enable`与A2/PiPER同步init、second-A进入PolicyActive、两次显式14秒`init→start→end→init` arm-goal轨迹、第一次L2+B回reset及第二次L2+B结束；live evidence为`.stage2_sessions/20260824_173406/evidence/live-both/20260825_004737_129916`，formal stop为`evidence/stop/20260825_005119_130865`。旧stop语义在第二次L2+B时立即quick-stop PiPER，故实测只有A2回趴地，PiPER未回开机休息位。新image `a2-piper-stage2:manual-arm-rest-stop-20260825`已在m45 build PASS：PolicyActive无显式arm命令时给actor零task command并保持PiPER manifest init；`stage2_gate.sh arm-goal`或`trajectory`才启用arm actor target；第二次L2+B同步让A2回实测趴地位、PiPER回本轮first-A锁存的启动休息位，完成后才stop。新语义尚待下一次hardware验证。

本轮已安全收尾：Stage2 live container停止，`no-lowcmd`计数0后A2通过guarded `SelectMode('ai_sport')`恢复官方mode，最终`CheckMode service='ai_sport'`；恢复evidence为`evidence/a2-restore/20260825_005440_131866`。随后PC2 bridge停止，m45无live container。固定SSH链路是本地`ssh m45`（`baoquanc@m45.precognition.team`）→ m45使用`/home/baoquanc/.ssh/id_ed25519_pc2_stage2`登录`unitree@192.168.123.162`；PC2现场根目录为`/home/unitree/Workspace/baoquanc`。

2026-08-24 23:19 HKT，PiPER standalone baseline最终PASS并由操作员批准。成功动作是10秒smoothstep到`[0,1.48,-0.63,-0.84,0,1.57] rad`、0.5度target验收、保持5秒、10秒返回本轮启动实测位姿、3.5度return验收后stop；成功evidence为`.stage2_sessions/20260824_173406/evidence/piper-baseline/20260824_231903_98613`。target最大误差`0.478 deg`，return最大误差`2.743 deg`。此前三个失败evidence保留了training-limit误用、mode handover未ready及过严return tolerance问题；最终bridge会读取controller limit feedback，enable先完成`arm_status=0/ctrl_mode=1`稳定handover，standalone remote支持Select/B quick stop与L2+B受控回程。

2026-08-24 23:26 HKT，PiPER baseline批准后的60秒dual shadow dry-run PASS：`.stage2_sessions/20260824_173406/evidence/dry-run/20260824_232605_100005`。首次dry-run failure `.stage2_sessions/20260824_173406/evidence/dry-run/20260824_232300_99064`不是policy故障，而是A2 baseline已恢复官方`ai_sport`后，全局`/lowcmd` observer将官方流量误判为Stage2输出且status topic尚未发现。Gate已改为让shadow node及observer共同使用隔离`/stage2_shadow/no_lowcmd`，status echo显式指定`std_msgs/msg/String`；真实A2/PiPER command topic没有shadow输出。dry-run尚待人工approval。

2026-08-24 23:41 HKT，Owner明确决定本session跳过过严的正式`joint-validation`人工表，不伪造approval receipt；`joint-observe`只读PASS evidence为`evidence/joint-observe/20260824_233131_101388`。随后process-stop fault PASS+APPROVED（`evidence/fault/20260824_233346_101892`），60秒coupled shadow PASS（`evidence/shadow/20260824_233514_102361`）。Owner授权中间receipt跳过并推进至live-both preflight。

完成版`config/site.yaml`已创建并由`docker/.env`选中：A2位置/rate采用已通过main standalone baseline的bundle边界；PiPER位置采用官方机械范围与本机controller宽泛`±180 deg`反馈的交集；PiPER rate采用实测`0.300 rad/s`，即50 Hz下`0.006 rad/tick`；deadline为20 ms、连续5次miss触发。最终image `a2-piper-stage2:dual-goal-pitch-trajectory-20260824`已build PASS；task-space arm goal在每次进入`PolicyActive`时以6秒smoothstep执行`[0.4,1.0472,0] → [0.4,-1.2566,0]`。终点超出manifest pitch训练采样下界`-1.0 rad`，但不是PiPER关节角，按训练端目标保留，J1–J6输出仍受site机械/rate limit。status新增goal、arm actor body pitch/roll plan及A2实测roll/pitch。隔离`live-preflight-both`最终PASS，evidence为`evidence/live-preflight-both/20260824_234854_107096`，明确报告`site=verified`且A2/PiPER output均not_published。真正live尚未批准或启动。

2026-08-24 23:53 HKT首次live启动中，第一条命令已创建正确image的`stage2-live-20260824_173406`并停在`IdleBlocked`零输出；随后两次重复启动因固定container name冲突而exit 1，暴露旧runner在检查同名容器前先做A2 release。现已改为hardware action前拒绝重复live。Owner要求简化操作后，最终候选改为`a2-piper-stage2:dual-triggered-trajectory-20260824`：第一次`A`自动且仅一次调用PiPER resume/enable后执行init；第二次`A`只进入PolicyActive并保持goal起点；`./scripts/stage2_gate.sh trajectory --operator baoquanc`才启动6秒轨迹。隔离验证显示`goal_trajectory_state=armed`、A2/PiPER output均not_published，非PolicyActive触发被拒绝；evidence为`evidence/triggered-trajectory-preflight/20260825_000056_112019`。

2026-08-25 00:02 HKT首次first-A尝试：direct log确认resume/enable成功，但没有`PiPER command gate open: starting synchronized...`，A2/PiPER未进入init，操作员在second A前退出。旧runner的Ctrl+C未传递到后台compose，残留live container由`stage2_gate.sh stop`双路径清理PASS，evidence `evidence/stop/20260825_000456_113403`。PC2随后为gate closed、ctrl_mode 0。最初推测command QoS不兼容，经真实ROS graph否定：原Stage2 publisher与PC2 subscriber实际均为BEST_EFFORT；失败检查保留在`evidence/piper-command-qos-preflight/20260825_000726_114971`，不得引用为根因。最终image `a2-piper-stage2:dual-triggered-handoverfix-20260825`改为以成功enable response直接seed本轮fresh gate-open，使下一20 ms tick进入init；command QoS保持双方BEST_EFFORT；runner为INT/TERM增加正式dual-path stop trap。Build与isolated no-output graph/site PASS evidence为`evidence/handover-fix-preflight/20260825_000936_116144`。

2026-08-25 00:12 HKT第二次first-A实机尝试中，PiPER resume/enable和handover seed均成功，但约263 ms后diagnostics报告gate重新关闭，A2仍未进入init；操作员在second A前`Ctrl+C`，新版trap完成双路径stop且无残留，live/stop evidence分别为`evidence/live-both/20260825_001224_116559`与`evidence/stop/20260825_001300_117022`。根因是direct executable使用single-threaded `rclcpp::spin`，约1 kHz A2 LowState加PiPER state/diagnostics/service callbacks使50 Hz control timer饿死并越过PiPER 200 ms command watchdog；不是init值、command QoS或`ai_sport`控制权交接。最终image `a2-piper-stage2:dual-triggered-executorfix-20260825`把control timer与trajectory service放入独立mutually-exclusive callback group，并用2-thread `MultiThreadedExecutor`运行；真实A2/PiPER输入负载下8秒isolated shadow收到35条status且隔离A2/PiPER output均零消息，evidence为`evidence/executor-shadow-preflight/20260825_001628_118549`。00:19 HKT再次只读实测`CheckMode`为空、5秒`/lowcmd`计数0且无live container，明确排除本次问题来自官方mode未release。每次`stage2_gate.sh live`仍在创建node前执行`motion-check → motion-release`，所以空档期`ai_sport`即使自动恢复也会在下一次live重新release；formal stop本身不等同于restore官方mode。

2026-08-25 00:20 HKT第三次live中，executor修复后first A使A2成功完成init；second A后A2出现一次剧烈joint target接管并保持明显正pitch，PiPER则quick-stop/gate closed。操作员随后触发trajectory，并用两段L2+B完成reset hold、PiPER stop与A2 prone interpolation，最终`Ctrl+C` exit 130且formal stop无残留。Live/stop evidence为`evidence/live-both/20260825_002043_119337`与`evidence/stop/20260825_002234_120016`。m45与PC2实测时钟相差约36.5秒；对齐PC2 bridge log后，PiPER关闭原因是enable后约0.15秒出现`arm_status=5`（SDK定义为joint communication abnormal），不是command timeout。随后只读CAN `0x2A1`为`0000010000000000`，确认当前arm_status/err_code恢复正常。PC2 bridge image `a2-piper-pc2-bridge:stable-enable-20260825`把MIT/high-follow enable放行条件从约15 ms正常样本收紧为连续0.5秒`arm_status=0 && ctrl_mode=1`；运行期任何非零arm_status仍立即quick-stop。重启后command gate closed、arm_status/ctrl_mode=`0/0`、joint/status 200 Hz。

Policy host最终image更新为`a2-piper-stage2:dual-roundtrip-handover-20260825`：second A后的前50个policy tick用smoothstep从manifest init joint hold接管actor target；PolicyActive未trigger时保持init task-space goal `[0.6,0,0]`，单次trajectory依次执行4秒`init → [0.4,1.0472,0]`、6秒到`[0.4,-1.2566,0]`、4秒回init。真实A2/PiPER state command-remapped shadow收到36条ready status，确认`goal_duration_s=14`且两路隔离command均零字节，evidence为`evidence/roundtrip-handover-preflight/20260825_002957_122308`。该验证不等同于再次实机enable。

2026-08-25 00:36 HKT第四次first-A尝试未进入init；操作员未按second A并`Ctrl+C`，formal stop PASS，live/stop evidence为`evidence/live-both/20260825_003656_125155`与`evidence/stop/20260825_003710_125606`。PC2此次明确记录`command_timeout`。根因是stable-enable bridge在单线程service callback内连续检查0.5秒mode，期间暂停PiPER state发布；Stage2把这个预期的>200 ms handover空窗当普通运行stale fault并重置为IdleBlocked，enable成功后已无first-A lifecycle发送首帧。最终policy image `a2-piper-stage2:handover-stale-fix-20260825`只在`InitHoldWaitingForPiperGate && enable request in flight`阶段保留lifecycle并持续发布锁存A2/PiPER hold；正常运行期200 ms stale fault规则不变。Fixture将A2 state保持50 Hz并让dummy PiPER owner阻塞0.70秒，验证`saw_enable_hold=true`、`saw_standup=true`，enable返回后约18 ms进入同步init，全部输出仅在`/fixture/*`；PASS evidence为`evidence/handover-stale-preflight/20260825_004327_128268`。

`docs/operator_runbook_zh_CN.md` 已收敛为这套现场设备的恢复手册：固定m45 repo/NIC/IP、PC2隔离目录、CAN/bridge恢复命令、实时state evidence与本次人工mapping观察记录。2026-08-24 21:35 HKT实测`CheckMode`为`form='0', name='ai', service='ai_sport'`；操作员授权后受保护`ReleaseMode ret=0`，随后mode为空且5秒`/lowcmd`计数为0，确认此前约1000 Hz bare-DDS流量来自宇树官方控制链。Evidence：`.stage2_sessions/20260824_173406/evidence/a2-motion-release/20260824_213519`。当前保留released状态，receipt顺序的唯一下一项是A2 standalone baseline；在limits/watchdog evidence完成前不创建或切换`config/site.yaml`。

2026-08-24 22:18 HKT按`krushell/piper_sdk`的`piper_sdk/demo/V2/piper_set_mit.py`将PiPER位置命令改为`MotionCtrl_2(1,1,0,0xAD)`后接`JointCtrl`：这是MOVE J + MIT/high-follow flag，不是逐电机`JointMitCtrl`。m45候选`a2-piper-stage2:mit-high-follow-20260824`与PC2候选`a2-piper-pc2-bridge:mit-high-follow-20260824`均build PASS；PC2当前运行候选bridge，diagnostics报告`move_j_mit_high_follow`且command gate closed。隔离和全局observer均确认无A2 LowCmd、无PiPER joint command；未调用enable/resume。Evidence：`.stage2_sessions/20260824_173406/evidence/mit-high-follow/20260824_221815`。

2026-08-24 22:43 HKT，A2 baseline的stand-up、PolicyActive、controlled-down、无残留policy container及官方`ai_sport`恢复已由`evidence/a2-baseline-audit/20260824_223627_85684`审计PASS，操作员随后签署`approvals/a2-baseline.receipt`。PiPER baseline被定向实现为：锁存启动实测位姿，5秒smoothstep到`[0,1.48,-0.63,-0.84,0,1.57] rad`，保持5秒，5秒返回启动位姿，到位后stop；不调用resume。image `a2-piper-stage2:piper-roundtrip-baseline-20260824`已build PASS；只读预检确认state/diagnostics正常、gate closed且未发command，evidence为`evidence/piper-roundtrip-preflight/20260824_224332`。预检同时修正Humble中`DiagnosticStatus.level`以单字节值到达客户端时的解码。当前唯一下一项是由现场操作员执行PiPER baseline。

2026-08-24已按main A2实际two-A实现补齐Stage2 pre-policy init：用户给出的A2与PiPER 1–6 init值和bundle manifest完全一致；第一次`A`锁存当前position，arm路径先保持measured pose并等待fresh PiPER command-gate-open diagnostics，随后A2/PiPER同步用300 tick约6秒插值到manifest init；第二次`A`只触发30-frame约0.60秒history warmup，下一tick进入PolicyActive，不存在固定3秒delay。`arm_j7/j8=0`仅是训练固定gripper target，PC2 bridge v1不发布它们。代码尚未触发任何实机enable或command。

候选tag `a2-piper-stage2:preenable-init-20260824`已在m45定向build PASS；真实state shadow ready且隔离A2/PiPER command observer均零消息，evidence为`.stage2_sessions/20260824_173406/evidence/pre-enable-init/20260824_212010`。当时全局no-lowcmd FAIL现已确认为release前官方`ai_sport`预期流量；candidate结果仍不升级任何Gate，也不证明hardware lifecycle。

Stage2新增显式恢复入口：`STAGE2_ALLOW_A2_RESTORE=1 ./scripts/stage2_gate.sh restore-a2 --iface enp130s0 --operator baoquanc`。它只允许在live container停止后执行，并依次要求`no-lowcmd 5`、guarded `motion-restore`到`ai_sport`、`motion-check`回到`service='ai_sport'`。本轮未执行恢复，因为后续自研policy Gate需要保持released状态。

2026-08-24 21:47 HKT从当前静止趴地A2只读采集5秒/5263个有效LowState样本，各轴range均不超过`0.0001 rad`。Training-order趴地目标固定为`[0.3602,-0.3789,0.3382,-0.3506,1.1862,1.1942,1.2177,1.1831,-2.7570,-2.7380,-2.7485,-2.7468] rad`。Stage2 normal stop已实现为：第一次L2+B让A2/PiPER用250 tick约5秒回manifest reset并保持；A重新做30-frame warmup后恢复policy；第二次L2+B才调用PiPER stop并让A2用250 tick约5秒插值到上述趴地目标。`Select`仍是immediate zero stop。Candidate `a2-piper-stage2:preenable-stop-lifecycle-20260824` build与isolated no-output PASS，evidence为`.stage2_sessions/20260824_173406/evidence/pre-enable-stop-lifecycle/20260824_220228`；两段动作尚未hardware验证。

## Entry

- `memory/stage2_dual_policy/description.md`
  - 真实 policy bundle、dog/arm contract、arm-first same-tick preview、direct A2 path、offline verification、site config 和现场 Gate。
- 需要判断剩余现场项时读取同目录的 `TODO.md`；已完成事实见 `DONE.md`。

## Routing

- 修改 `deploy/a2_piper_stage2/**` 前先读本文件，再读 `memory/stage2_dual_policy/description.md`。
- 修改 raw A2 `LowState` / `LowCmd` 或 `A2LowLevelInterface` 时回到 `../../ros2/A2/MEMORY.md`。
- 修改 `/piper/*`、SocketCAN、PiPER watchdog 或 PC2 hardware owner 时回到 `../../ros2/Piper/MEMORY.md`。
- 全局入口是 `../../MEMORY.md`。
