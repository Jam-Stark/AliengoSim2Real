// GaitClock is header-only (see gait_clock.h).
// This file exists only so the build system has a .cpp to compile for the
// translation unit if future non-inline methods are added.

#include "aliengo_deploy/gait_clock.h"
