# TODO

## 2026-06-04 14:48 HKT

- [ ] 新增 robot、runtime 或 deployment subsystem 时，同步更新 root `MEMORY.md`、`memory/MEMORY.md` 和 top-level `README.md` routing。
- [ ] 当各 subsystem 的 verified environment 固化后，在 global README 增加 build/test matrix，避免只依赖口头环境假设。
- [ ] 在最终 Ubuntu policy host 完成 Stage2 image build，并按 operator runbook 生成 A2/PiPER 现场 Gate receipts。
